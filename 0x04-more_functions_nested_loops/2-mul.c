#include "main.h"
/**
 * mul - Mulitiplies two integers
 * @x: The first number to multiply
 * @y: The second number to multiply with first number
 * Return: The result of mulitiplication between x param and y param
 */
int mul(int x, int y)
{

return (x * y);
}
